/* * NombreDeProyecto * ========================================================
   Global javascript [/src/js/global.produ.js]
   ========================================================================== */

/* // Site variables -------------------------------------------------------- */
// Variables globales para el entorno produccion

var dir_env_js = '';

/* // REF [50] Google captcha
 * La variable captcha_key_site tambien debe ser declarada
 * en `[/src/var/form.var.inc.php]` */
let captcha_key_site = 'sinclave';



$(document).ready(function () {    
/* // Separator ------------------------------------------------------------- */
});

document.addEventListener("DOMContentLoaded", () => {
/* // Separator ------------------------------------------------------------- */
});

window.addEventListener("load", (event) => {
});

window.addEventListener("load", function(event) {
});

$(window).scroll(function () {
/* // Separator ------------------------------------------------------------- */
});